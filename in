LEFT JOIN (
    select distinct DSID, ENVIRONMENT_ID, RUNID, ACTV_IND, MetricId, MetricName
    from TEMP_DSE_TESTRESULTS_STG
    where DSID = :DSID
      and ENVIRONMENT_ID = :v_ENVIRONMENT_ID
      and RUNID = :v_MAX_RUNID
      and ACTV_IND = 'Y'
) DPR
